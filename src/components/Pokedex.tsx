import Pokelist from "./Pokelist";

const Pokedex: React.FC<{}> = ({ onPokeData }) => {
  return (
    <div>
      {/* search */}
      {/* pokeList */}
      <Pokelist />
      {/* pokeSearch */}
    </div>
  );
};

export default Pokedex;
